# DiegoMont.github.io
Personal Portfolio Webpage

### Required variables
Set environment variables or create a *.env* file.

- `VITE_ESTATE_API_URL` — ORDS endpoint used in `/house` to fetch real estate data. The base ORDS URL can be found inside the wallet's README. This endpoint must end with `ords/real-estates/real-estates/estates`.
